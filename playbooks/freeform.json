{
  "id": "freeform",
  "name": "Freeform — the named escape hatch",
  "description": "One permissive stage for operational and investigative spawns; createWorktree:false gives the worker no worktree, so nothing it writes can be reviewed or landed as a unit.",
  "entryStages": ["freeform"],

  "stages": {
    "freeform": {
      "description": "For a one-off with nothing to plan or review — answer a question, read a log, run a command. Brief the goal and its sentinel. The worker is in the live project tree, so the moment the job turns into code changes, stop and re-run it under a playbook that reviews and lands.",
      "workers": "many",
      "tools": {
        "spawn_instance": { "require": { "createWorktree": false } }
      }
    }
  },

  "transitions": []
}
