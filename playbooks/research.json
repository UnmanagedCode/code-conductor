{
  "id": "research",
  "name": "Research — investigative fan-out",
  "description": "Many investigators in parallel, none with a worktree of its own.",
  "entryStages": ["investigate"],

  "stages": {
    "investigate": {
      "description": "Fan out one investigator per independent question in a single turn, each briefed to answer in prose with file/line citations. They work in the live project tree, so \"change nothing\" must be in your brief — nothing enforces it. Take anything worth changing into a run that can land.",
      "workers": "many",
      "tools": {
        "spawn_instance": { "require": { "createWorktree": false } },
        "sync_worktree": "deny"
      }
    }
  },

  "transitions": []
}
