// Hand-written declarations for the still-`.js` src/projects.js module,
// covering exactly the exports the converted modules import. Grows only when a
// future converted module needs more of projects.js — do not speculative-type
// the rest of it here.
export function orchStoreRoot(): string;
