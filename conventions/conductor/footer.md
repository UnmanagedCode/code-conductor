---

This doc is `.conduct/CONVENTIONS.md`, generated from `conventions/conductor/core.md` + `conventions/conductor/*.md` in the code-conductor repo and rewritten before every conductor spawn — never hand-edit it. Edits to the built-in text go through a spawned worker on those fragments; convention toggles and custom conventions are managed in the orchestrator's **Settings → Conductor conventions**.
