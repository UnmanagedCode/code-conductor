---

This doc is generated from `conventions/conductor/core.md` + `conventions/conductor/*.md` in the code-conductor repo (`.conduct/CONDUCT.md` is regenerated on boot and on settings change, not hand-written), so edits to the built-in text go through a spawned worker on those fragments; convention toggles and custom conventions are managed in the orchestrator's **Settings → Conductor conventions**.
