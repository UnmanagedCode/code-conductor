---

This doc is generated from `conduct/core.md` + `conduct/modules/*.md` in the code-conductor repo (`.conduct/CONDUCT.md` is regenerated on boot and on settings change, not hand-written), so edits to the built-in text go through a spawned worker on those fragments; module toggles and custom modules are managed in the orchestrator's **Settings → Conductor conventions**. The only files you may edit directly are `.conduct/CLAUDE.md` and `.conduct/tasks/*.md`.
